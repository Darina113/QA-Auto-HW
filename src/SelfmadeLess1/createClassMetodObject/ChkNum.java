package SelfmadeLess1.createClassMetodObject;
//создадим класс и метод с принимающим значением (продолжение ParamDemo)
public class ChkNum {
    //метод с принимающими параметрами и возвращает тру или фолс
    //int x -принимающий параметры
    boolean isEven(int x){
        if (x%2==0) return true;
        else return false;
    }
}
